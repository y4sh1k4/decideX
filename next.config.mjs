/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['shorturl.at',"olive-fashionable-mule-815.mypinata.cloud"],  // Add the external domains you want to allow
      },
};

export default nextConfig;

